// function type expression
const someFunction: (param: number) => number = (param) => param;

// parameter type annotation
const exampleOne = (param: number) => {
  // do something...
};

// return type annotation
const exampleTwo = (param: number): number => {
  return param;
};
